/target/
**/*.rs.bk
Cargo.lock
.vercel
# binaires locaux générés par build
polygone
health_server
webui
node
*.profraw *.profdata
target/

# Pre-built binaries — served via GitHub Releases, not in git
dist/polygone-bin/polygone
